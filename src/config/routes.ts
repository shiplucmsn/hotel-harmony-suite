export const PUBLIC_ROUTES = ["/", "/login", "/register", "/forgot-password", "/reset-password", "/verify-email", "/two-factor", "/pricing"] as const;

export const AUTH_ROUTES = ["/login", "/register", "/forgot-password", "/reset-password"] as const;

export const APP_PREFIX = "/app";

export const DEFAULT_APP_ROUTE = "/app/dashboard";

export const DEFAULT_AUTH_ROUTE = "/login";
