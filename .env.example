# Dev (Vite on :8080): use /api — proxied to Laravel :8000. Or set http://127.0.0.1:8000/api directly.
VITE_API_BASE_URL=/api
# Raise if slow local SMTP/network; ticket replies now queue email after response (default 15s).
VITE_API_TIMEOUT_MS=30000
